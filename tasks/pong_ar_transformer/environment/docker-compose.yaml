services:
  main:
    runtime: nvidia
    build:
      context: ..
      dockerfile: environment/Dockerfile
      args:
        HOST_UID: ${HOST_UID:-1007}
        HOST_GID: ${HOST_GID:-1007}
    shm_size: 8g
    environment:
      - PYTHONPATH=/opt/autoworldbench/lib:/opt/autoworldbench:/app/tasks/pong_ar_transformer
      - TASK_ID=pong_ar_transformer
      - GAME_ID=pong
      - MODEL_TYPE=ar_transformer
      - MAX_TRAIN_SECONDS=600
    volumes:
      - ${HOME}/.aws:/home/agent/.aws:ro
      - ${HOME}/.claude:/home/agent/.claude:ro
